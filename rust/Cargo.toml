# Cargo.toml

[package]
name = "resonant_search"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.9.6" # Use a specific version
primal = "0.3.1" # Use the version without the problematic feature
scraper = "0.17.1" # Remove features = ["html5ever"]